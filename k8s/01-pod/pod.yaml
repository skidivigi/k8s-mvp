apiVersion: v1
kind: Pod
metadata:
  name: hello-pod
  namespace: lab
  labels:
    app: hello
spec:
  containers:
    - name: hello-container
      image: nginx:1.27
      ports:
        - containerPort: 80
